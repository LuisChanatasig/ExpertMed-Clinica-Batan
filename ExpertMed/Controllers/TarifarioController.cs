using Microsoft.AspNetCore.Mvc;

namespace ExpertMed.Controllers
{
    public class TarifarioController : Controller
    {
        //[HttpGet]
        //public IActionResult GetByDescripcion(string descripcion, int insuranceCompanyId)
        //{
        //    var resultado = _tarifarioService.ObtenerPorDescripcionYSeguro(descripcion, insuranceCompanyId);

        //    if (resultado == null)
        //        return Json(new { precio = 0, precio_aseguradora = 0 });

        //    return Json(new
        //    {
        //        precio = resultado.PrecioUnitario,
        //        precio_aseguradora = resultado.ValorAseguradora
        //    });
        //}

    }
}
